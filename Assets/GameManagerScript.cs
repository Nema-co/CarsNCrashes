using Unity;
using UnityEngine;

public class GameManagerScript : MonoBehaviour
{
    // Start is called before the first frame update

    private static int MaxPlayerCount;
    private static int isSplitScreen;
    void Start()
    {
      //MaxPlayerCount = GameData.PlayerCount;
    }

}
